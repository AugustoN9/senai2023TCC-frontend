import React, { useState } from "react";
import examesMock from "../../mock/examesMock";

const ExameList = () => {
    const [exams, setExams] = useState(examesMock);
    
    return(

    )
}

export default ExameList;